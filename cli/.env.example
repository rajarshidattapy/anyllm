# OpenAI API key for distillation (required for full-quality snapshots)
# Without this, `anyllm pack` runs in offline fallback mode with low-confidence output.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
